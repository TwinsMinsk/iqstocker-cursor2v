# 🔧 Решение проблемы подключения к Supabase

## ❌ Проблема

При запуске бота возникает ошибка:
```
socket.gaierror: [Errno 11001] getaddrinfo failed
```

Это означает, что локальная машина не может разрешить DNS имя для Supabase PostgreSQL хоста.

## ✅ Решения

### Решение 1: Добавить pool_pre_ping и SSL настройки (УЖЕ ПРИМЕНЕНО)

Обновлен `src/database/connection.py`:
- Добавлен `pool_pre_ping=True` для проверки соединений
- Добавлены `connect_args` с SSL настройками
- Добавлены таймауты и пул соединений

### Решение 2: Проверить DNS и интернет

Если проблема с DNS сохраняется:

1. **Проверьте интернет-соединение:**
   ```powershell
   Test-NetConnection -ComputerName db.zpotpummnbfdlnzibyqb.supabase.co -Port 5432
   ```

2. **Проверьте DNS:**
   ```powershell
   Resolve-DnsName db.zpotpummnbfdlnzibyqb.supabase.co
   ```

3. **Проверьте firewall:**
   Убедитесь, что порт 5432 не заблокирован

### Решение 3: Использовать прямую загрузку данных через Supabase MCP

Если локальное подключение не работает, можно использовать Supabase MCP для работы с данными.

### Решение 4: Временно использовать SQLite для локального тестирования

Для тестирования бота можно временно использовать SQLite:

```python
# В src/config/settings.py
DATABASE_URL="sqlite+aiosqlite:///./local.db"
```

## 📊 Текущий статус

✅ Исправления применены:
- Pool pre-ping добавлен
- SSL настройки добавлены
- Таймауты настроены

⏳ Нужно проверить:
- Работает ли подключение после исправлений
- Нужно ли дополнительно настраивать DNS/firewall

## 🚀 Следующие шаги

1. Перезапустить бота
2. Протестировать подключение
3. Если проблема сохраняется - использовать альтернативные решения

