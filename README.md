# IQStocker v2.0

Telegram бот для анализа портфолио Adobe Stock с подписками, реферальной программой и админ-панелью.

## 📋 Описание

IQStocker v2.0 - это полнофункциональный Telegram бот, который помогает сток-фотографам:
- 📊 Анализировать продажи через загрузку CSV файлов
- 🎨 Генерировать актуальные темы для контента
- 📚 Обучать через подробные уроки
- 📅 Планировать контент через календарь
- 🎁 Зарабатывать на реферальной программе

## 🏗️ Архитектура

Проект построен на чистой архитектуре с разделением на слои:
- **Handlers** - обработчики команд Telegram бота
- **Services** - бизнес-логика
- **Repositories** - доступ к данным
- **Models** - SQLModel модели данных

## 🛠️ Технологический стек

- **Python 3.11+**
- **Aiogram 3.4+** - Telegram Bot framework
- **FastAPI 0.109+** - Admin panel + API
- **SQLModel 0.0.16+** - ORM + Pydantic
- **PostgreSQL 16** - основная база данных
- **Redis 7.2** - кеширование и ARQ
- **ARQ 0.26+** - async task queue
- **Alembic** - миграции базы данных
- **Docker + Docker Compose** - контейнеризация

## 📁 Структура проекта

```
iqstocker-v2/
├── src/
│   ├── bot/              # Telegram Bot
│   │   ├── handlers/     # Обработчики команд
│   │   ├── keyboards/    # Клавиатуры
│   │   ├── states/       # FSM состояния
│   │   └── lexicon/      # Тексты бота
│   ├── admin/            # FastAPI Admin Panel
│   ├── api/              # API endpoints
│   ├── database/         # Database layer
│   ├── services/         # Business logic
│   ├── workers/          # ARQ workers
│   └── core/             # Core utilities
├── alembic/              # Database migrations
├── tests/                # Tests
└── scripts/              # Utility scripts
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
poetry install
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

Необходимые переменные:
- `BOT_TOKEN` - токен Telegram бота
- `CHANNEL_ID` - ID канала для подписки
- `ADMIN_IDS` - список ID администраторов
- `DATABASE_URL` - URL подключения к PostgreSQL
- `REDIS_HOST`, `REDIS_PORT` - настройки Redis
- `ADMIN_USERNAME`, `ADMIN_PASSWORD` - учетные данные админ-панели
- `SECRET_KEY` - секретный ключ для JWT

### 3. Запуск с Docker Compose

```bash
docker compose up -d
```

Это запустит:
- PostgreSQL (порт 5432)
- Redis (порт 6379)
- Bot service
- Admin service (порт 8000)
- Worker service

### 4. Применение миграций

```bash
poetry run alembic upgrade head
```

### 5. Загрузка тем из CSV

```bash
poetry run python scripts/load_themes.py
```

## 📊 Модели данных

### Основные таблицы:
1. **users** - пользователи бота
2. **limits** - лимиты использования
3. **csv_analyses** - загруженные CSV файлы
4. **analytics_reports** - сгенерированные отчеты
5. **theme_templates** - шаблоны тем для генерации
6. **theme_requests** - выданные темы пользователям
7. **payments** - платежи через Tribute.tg
8. **system_messages** - системные сообщения
9. **broadcast_messages** - массовые рассылки

## 🎯 Основные функции

### Для пользователей:
- 📊 Анализ CSV файлов с продажами Adobe Stock
- 🎨 Генерация тем для контента по категориям
- 📚 Обучающие материалы
- 📅 Календарь генераций
- 🎁 Реферальная программа с IQ баллами
- 💳 Подписки PRO и ULTRA

### Для администраторов:
- 👥 Управление пользователями
- 💳 Управление платежами
- 📊 Статистика и аналитика
- 📢 Массовые рассылки
- 🎨 Редактирование лексикона бота

## 🔧 Разработка

### Запуск бота локально:

```bash
poetry run python -m src.bot.main
```

### Запуск админ-панели:

```bash
poetry run uvicorn src.admin.main:app --reload
```

### Запуск worker:

```bash
poetry run arq src.workers.main.WorkerSettings
```

### Тестирование:

```bash
poetry run pytest
```

### Линтинг:

```bash
poetry run ruff check src/
poetry run ruff format src/
```

### Проверка типов:

```bash
poetry run mypy src/
```

## 📝 Миграции

### Создание миграции:

```bash
poetry run alembic revision --autogenerate -m "Описание изменений"
```

### Применение миграций:

```bash
poetry run alembic upgrade head
```

### Откат миграции:

```bash
poetry run alembic downgrade -1
```

## 🔐 Безопасность

- Все секреты хранятся в `.env` файле (не коммитится в git)
- Webhook подписи проверяются через HMAC
- SQL injection защита через параметризованные запросы
- Rate limiting для API endpoints

## 📦 Деплой

Проект настроен для деплоя на Railway.app:
- Автоматические миграции при деплое
- Health checks для всех сервисов
- Environment variables через Railway dashboard

## 📄 Лицензия

Proprietary - все права защищены

## 👥 Команда

IQStocker Team

---

**Версия:** 2.0.0  
**Дата обновления:** 2024-01-15

