# IQStocker v2.0 - Полный Анализ Проекта

## 📊 Статус проекта

### ✅ Что уже готово

1. **Документация (100%)**:
   - ✅ `tid_v2.md` - полная техническая спецификация (1144 строки)
   - ✅ `AGENT_PLAN.md` - план реализации для AI агента
   - ✅ `TRIBUTE_SETUP_GUIDE.md` - гайд по интеграции Tribute.tg
   - ✅ `QUICKSTART.md` - быстрый старт
   - ✅ `.cursor/rules/` - 10 MDC файлов для контекста AI

2. **Конфигурация (100%)**:
   - ✅ `pyproject.toml` - Poetry зависимости (все готово)
   - ✅ `docker-compose.yml` - Docker конфигурация для разработки
   - ✅ `Dockerfile.bot`, `Dockerfile.admin`, `Dockerfile.worker` - готовы
   - ✅ `scaffold.sh` - скрипт создания структуры

3. **Лексикон (100%)**:
   - ✅ `src/bot/lexicon/lexicon_ru.py` - все тексты бота (681 строка)

4. **Структура проекта (частично)**:
   - ✅ Созданы основные директории
   - ❌ Нет файлов кода (только структура)

---

## 🔑 Что нужно для разработки (обязательно)

### 1. Telegram Bot

#### BOT_TOKEN
- **Что это**: Токен Telegram бота от @BotFather
- **Где получить**: 
  1. Написать @BotFather в Telegram
  2. Команда `/newbot`
  3. Следовать инструкциям
- **Формат**: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
- **Куда добавить**: `.env` → `BOT_TOKEN=your_token_here`
- **⚠️ Критично**: Без этого бот не запустится

#### CHANNEL_ID
- **Что это**: ID Telegram канала для обязательной подписки
- **Где получить**: 
  1. Добавить бота в канал как администратора
  2. Отправить любое сообщение в канал
  3. Использовать `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
  4. Найти `chat.id` в ответе (обычно начинается с `-100`)
- **Формат**: `-1001234567890`
- **Куда добавить**: `.env` → `CHANNEL_ID=-1001234567890`
- **⚠️ Важно**: Без подписки на канал бот не будет работать

#### ADMIN_IDS
- **Что это**: ID администраторов бота (через запятую)
- **Где получить**: 
  1. Написать боту @userinfobot
  2. Скопировать ваш Telegram ID
- **Формат**: `123456789,987654321`
- **Куда добавить**: `.env` → `ADMIN_IDS=123456789,987654321`
- **⚠️ Важно**: Для доступа к админ-панели

---

### 2. База данных

#### DATABASE_URL
- **Что это**: URL подключения к PostgreSQL
- **Для локальной разработки**:
  - Формат: `postgresql+asyncpg://user:pass@localhost:5432/iqstocker`
  - Автоматически из `docker-compose.yml` (не нужно настраивать)
- **Для Railway (production)**:
  - Railway автоматически создаст после `railway add --database postgresql`
  - Автоматически добавится в ENV переменные
- **⚠️ Важно**: В локальной разработке используем Docker Compose, переменная не нужна

---

### 3. Redis

#### REDIS_URL или REDIS_HOST + REDIS_PORT
- **Что это**: URL подключения к Redis для ARQ
- **Для локальной разработки**:
  - Формат: `redis://localhost:6379/0`
  - Автоматически из `docker-compose.yml` (не нужно настраивать)
- **Для Railway (production)**:
  - Railway автоматически создаст после `railway add --database redis`
  - Автоматически добавится в ENV переменные
- **⚠️ Важно**: В локальной разработке используем Docker Compose, переменная не нужна

---

### 4. Tribute.tg Payment (КРИТИЧНО)

#### TRIBUTE_API_KEY
- **Что это**: API ключ для интеграции платежей
- **Где получить**: 
  1. Зарегистрироваться на https://tribute.tg/docs
  2. Создать merchant аккаунт
  3. Dashboard → Settings → API → Generate API Key
- **Формат**: `trib_abc123def456...`
- **Куда добавить**: `.env` → `TRIBUTE_API_KEY=your_api_key_here`
- **⚠️ КРИТИЧНО**: Без этого платежи не будут работать

#### TRIBUTE_WEBHOOK_SECRET
- **Что это**: Секретный ключ для проверки webhook подписей
- **Где получить**: 
  1. Dashboard → Settings → API → Generate Webhook Secret
- **Формат**: `wh_sec_abc123def456...`
- **Куда добавить**: `.env` → `TRIBUTE_WEBHOOK_SECRET=your_webhook_secret_here`
- **⚠️ КРИТИЧНО**: Без этого webhook'и не будут безопасными

#### TRIBUTE_MERCHANT_ID (опционально)
- **Что это**: ID мерчанта (если требуется)
- **Где получить**: Dashboard → Settings
- **Формат**: `merchant_123...`
- **Куда добавить**: `.env` → `TRIBUTE_MERCHANT_ID=your_merchant_id_here`

#### TRIBUTE_BASE_URL
- **Что это**: Базовый URL API Tribute.tg
- **Значение**: `https://api.tribute.tg` (по умолчанию)
- **Куда добавить**: `.env` → `TRIBUTE_BASE_URL=https://api.tribute.tg`

#### BASE_URL
- **Что это**: Публичный URL вашего приложения для webhook'ов
- **Для локальной разработки**: `http://localhost:8000` (не будет работать для webhook)
- **Для Railway (production)**: 
  - Railway автоматически выдаст после деплоя
  - Формат: `https://your-app-name.railway.app`
  - Добавить в `.env` → `BASE_URL=https://your-app-name.railway.app`
- **⚠️ Важно**: Для webhook'ов Tribute.tg нужен HTTPS URL

---

### 5. Admin Panel

#### ADMIN_USERNAME
- **Что это**: Логин для доступа к админ-панели
- **Значение по умолчанию**: `admin`
- **Куда добавить**: `.env` → `ADMIN_USERNAME=admin`

#### ADMIN_PASSWORD
- **Что это**: Пароль для доступа к админ-панели
- **⚠️ КРИТИЧНО**: Изменить на свой безопасный пароль!
- **Куда добавить**: `.env` → `ADMIN_PASSWORD=your_secure_password_here`

#### SECRET_KEY
- **Что это**: Секретный ключ для JWT токенов и сессий
- **Как сгенерировать**: 
  ```bash
  openssl rand -hex 32
  # или
  python -c "import secrets; print(secrets.token_hex(32))"
  ```
- **Формат**: `abc123def456...` (64 символа)
- **Куда добавить**: `.env` → `SECRET_KEY=your_generated_secret_key_here`
- **⚠️ КРИТИЧНО**: Должен быть уникальным и случайным

---

### 6. Application Settings

#### ENVIRONMENT
- **Что это**: Окружение (development/production)
- **Значение**: `development` для локальной разработки
- **Куда добавить**: `.env` → `ENVIRONMENT=development`

#### LOG_LEVEL
- **Что это**: Уровень логирования
- **Значение**: `INFO` (или `DEBUG` для разработки)
- **Куда добавить**: `.env` → `LOG_LEVEL=INFO`

---

## 📝 Полный список ENV переменных

```bash
# ============================================================================
# ОБЯЗАТЕЛЬНЫЕ (для работы MVP)
# ============================================================================

# Telegram Bot
BOT_TOKEN=your_bot_token_here
CHANNEL_ID=-1001234567890
ADMIN_IDS=123456789,987654321

# Tribute.tg Payment (критично для платежей)
TRIBUTE_API_KEY=your_tribute_api_key_here
TRIBUTE_WEBHOOK_SECRET=your_webhook_secret_here
TRIBUTE_MERCHANT_ID=your_merchant_id_here  # опционально
TRIBUTE_BASE_URL=https://api.tribute.tg

# Admin Panel
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password_here
SECRET_KEY=your_generated_secret_key_here  # openssl rand -hex 32

# Application
ENVIRONMENT=development
LOG_LEVEL=INFO
BASE_URL=http://localhost:8000  # для локальной разработки

# ============================================================================
# АВТОМАТИЧЕСКИЕ (из Docker Compose / Railway)
# ============================================================================

# Database (автоматически из docker-compose.yml)
DATABASE_URL=postgresql+asyncpg://iqstocker:iqstocker@postgres:5432/iqstocker

# Redis (автоматически из docker-compose.yml)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Для production (Railway) эти переменные добавятся автоматически
```

---

## ❓ Уточнения и предложения по проекту

### 1. Реферальная система - детализация

**Текущее описание**: Баллы начисляются только при покупке PRO или ULTRA подписки рефералом.

**Вопросы для уточнения**:
1. Баллы начисляются только один раз за каждого реферала, или каждый раз при покупке?
   - Например: реферал покупает PRO, потом через месяц продлевает PRO - начисляется балл второй раз?

2. Что происходит, если реферал покупает PRO, а потом переходит на ULTRA?
   - Начисляется ли балл за переход на ULTRA, или только за первую покупку?

3. Сколько баллов начисляется, если реферал покупает сразу несколько месяцев (например, 90 дней)?
   - 1 балл за покупку, независимо от срока?
   - Или 3 балла за 90 дней?

**Предложение**: 
- **1 балл = 1 покупка подписки** (PRO или ULTRA)
- Каждая покупка (включая продление) = +1 балл
- Неважно, какой срок подписки (30/90 дней)

---

### 2. Обмен баллов - детализация

**Текущее описание**: 
- 1 балл → скидка 25%
- 2 балла → скидка 50%
- 3 балла → 1 месяц PRO бесплатно
- 4 балла → 1 месяц ULTRA бесплатно
- 5 баллов → доступ в закрытый канал

**Вопросы для уточнения**:
1. Когда пользователь обменивает баллы на скидку (1-2 балла):
   - Скидка применяется к текущей покупке сразу?
   - Или скидка сохраняется как промокод для следующей покупки?

2. Когда пользователь обменивает 3-4 балла на бесплатный месяц:
   - Подписка активируется сразу, или добавляется к текущей?
   - Что если у пользователя уже есть активная подписка?

3. Доступ в закрытый канал (5 баллов):
   - Это разовый доступ или постоянный?
   - Как технически предоставить доступ? (добавить в канал через bot.add_chat_member?)

**Предложение**:
- **Скидки (1-2 балла)**: применяются к текущей покупке сразу
- **Бесплатный месяц (3-4 балла)**: добавляется к текущей подписке (если есть) или создается новая на 30 дней
- **Доступ в канал (5 баллов)**: постоянный, бот добавляет пользователя в канал автоматически

---

### 3. CSV анализ - детализация формата

**Текущее описание**: Ожидается CSV файл с экспорта Adobe Stock.

**Вопросы для уточнения**:
1. Какой точный формат CSV от Adobe Stock?
   - Нужен пример реального CSV файла (минимум 5-10 строк)
   - Какие колонки точно есть: Date, Asset ID, Title, Type, Impressions, Downloads, Revenue?
   - Есть ли заголовок (header)?

2. Что делать, если формат отличается?
   - Показывать ошибку?
   - Пытаться определить формат автоматически?

3. Есть ли ограничения по размеру файла?
   - Предложение: максимум 10 MB, максимум 10,000 строк

**Предложение**:
- Поддержка нескольких форматов CSV (с заголовком и без)
- Автоматическое определение колонок по названиям
- Валидация формата перед обработкой
- Лимит: 10 MB, 10,000 строк

---

### 4. Генерация тем - источник данных

**Текущее описание**: Генерация тем для категорий (vectors, photos, videos, audio, templates).

**Вопросы для уточнения**:
1. Откуда брать темы?
   - Предзаполненная БД с темами?
   - AI генерация (какой API? OpenAI, Anthropic?)
   - Комбинация (БД + AI для вариаций)?

2. Сколько тем должно быть в БД для начала?
   - Предложение: минимум 50 тем на категорию (250 всего)

3. Как часто обновлять темы?
   - Добавление новых тем вручную через админ-панель?
   - Или автоматическая генерация?

**Предложение (для MVP)**:
- **Старт**: Предзаполненная БД с 50 темами на категорию (250 тем)
- **Структура темы**: `theme`, `description`, `relevance`, `keywords` (JSON массив)
- **Будущее**: Можно добавить AI генерацию позже

---

### 5. Уроки - содержание

**Текущее описание**: 4 раздела уроков.

**Вопросы для уточнения**:
1. Откуда брать контент уроков?
   - Тексты уже готовы?
   - Или нужно создать контент?

2. Какой формат уроков?
   - Только текст в боте?
   - Или ссылки на внешние материалы (YouTube, статьи)?

**Предложение (для MVP)**:
- **Старт**: Заглушки с описанием разделов
- **Структура**: Текст в боте + опциональные ссылки на внешние ресурсы
- **Будущее**: Можно добавить видео/файлы позже

---

### 6. Календарь генераций - источник данных

**Текущее описание**: Рекомендации на неделю/месяц для загрузок.

**Вопросы для уточнения**:
1. Откуда брать рекомендации?
   - Статические данные?
   - API для получения трендов?
   - Или просто примеры данных?

**Предложение (для MVP)**:
- **Старт**: Статические рекомендации (заглушки)
- **Структура**: JSON с данными по неделям/месяцам
- **Будущее**: Можно добавить реальную аналитику трендов

---

### 7. Админ-панель - редактирование лексикона

**Текущее описание**: "В жтом разделе мы имеем доступ ко всем сообщениям и кнопкам из lexicon_ru.py и можем их исправлять и корректировать"

**Вопросы для уточнения**:
1. Как технически это реализовать?
   - Редактирование файла `lexicon_ru.py` напрямую через веб-интерфейс?
   - Или отдельная таблица в БД для текстов?
   - Или оба варианта (файл для дефолтных, БД для кастомных)?

**Предложение**:
- **Для MVP**: Таблица `SystemTexts` в БД для переопределения текстов из лексикона
- **Логика**: 
  - Если текст есть в БД → использовать из БД
  - Если нет → использовать из `lexicon_ru.py`
- **Админ-панель**: CRUD интерфейс для редактирования текстов

---

### 8. Проверка подписки на канал

**Текущее описание**: Middleware для проверки подписки.

**Вопросы для уточнения**:
1. Проверять подписку на каждое действие или только при старте?
   - Предложение: при старте + периодическая проверка (раз в день)

2. Что делать, если пользователь отписался после регистрации?
   - Блокировать доступ сразу?
   - Или напоминать при следующем действии?

**Предложение**:
- Проверка при `/start` (обязательно)
- Проверка при первом действии каждый день
- Если не подписан → показать сообщение, блокировать функционал до подписки

---

### 9. Тестовые подписки

**Текущее описание**: Есть `TEST_PRO` в enum SubscriptionTier.

**Вопросы для уточнения**:
1. Для чего нужен TEST_PRO?
   - Для тестирования функционала?
   - Для бета-тестеров?

2. Как активировать TEST_PRO?
   - Через админ-панель?
   - Через специальную команду?

**Предложение**:
- TEST_PRO активируется только через админ-панель
- Имеет те же права, что и PRO
- Используется для тестирования и бета-тестеров

---

### 10. Миграции базы данных

**Текущее описание**: Использование Alembic для миграций.

**Вопросы для уточнения**:
1. Нужна ли начальная миграция с нуля?
   - Да, создам первую миграцию со всеми таблицами

2. Есть ли уже существующие данные для миграции?
   - Нет, проект новый

**Предложение**:
- Создам начальную миграцию со всеми 9 таблицами
- Все индексы и constraints согласно TID

---

## ✅ Чеклист для начала разработки

### Обязательные данные (нужны сейчас):
- [ ] **BOT_TOKEN** - от @BotFather
- [ ] **CHANNEL_ID** - ID Telegram канала
- [ ] **ADMIN_IDS** - ID администраторов
- [ ] **TRIBUTE_API_KEY** - от Tribute.tg
- [ ] **TRIBUTE_WEBHOOK_SECRET** - от Tribute.tg
- [ ] **ADMIN_PASSWORD** - безопасный пароль
- [ ] **SECRET_KEY** - сгенерированный ключ

### Желательные данные (можно добавить позже):
- [ ] Пример CSV файла от Adobe Stock
- [ ] Список тем для генерации (если есть)
- [ ] Контент для уроков (если есть)

---

## 🚀 Следующие шаги

После того как вы предоставите необходимые данные (токены и уточнения), я готов:

1. ✅ Создать полную структуру проекта
2. ✅ Реализовать все модели БД (9 таблиц)
3. ✅ Создать все repositories (8 репозиториев)
4. ✅ Реализовать все services (7 сервисов)
5. ✅ Создать все handlers (12 handlers)
6. ✅ Настроить ARQ workers
7. ✅ Создать админ-панель (FastAPI)
8. ✅ Настроить интеграцию с Tribute.tg
9. ✅ Создать миграции Alembic
10. ✅ Добавить тесты (unit + integration)
11. ✅ Настроить Docker конфигурацию
12. ✅ Подготовить к деплою на Railway

**Ожидаю от вас**:
1. Все токены и ключи (из раздела "🔑 Что нужно для разработки")
2. Ответы на вопросы из раздела "❓ Уточнения и предложения"

После получения этих данных, я приступлю к полной реализации проекта! 🎯

